import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  titleOfList: {
    fontSize: 30,
    fontWeight: 'bold',
  },
  spinnerContainer: {
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
    alignSelf: 'center',
  },
  searchContainer: {
    marginTop: 10,
    marginHorizontal:10
  },
});
